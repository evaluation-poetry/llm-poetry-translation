[project]
name = "reasoning-poetry-translation"
version = "0.1.0"
description = "Research code for evaluating reasoning and non-reasoning LLM settings in modern Chinese poetry translation."
requires-python = ">=3.10"
dependencies = [
  "requests>=2.31",
  "pandas>=2.0",
  "sacrebleu>=2.4",
  "bert-score>=0.3.13",
  "unbabel-comet>=2.2",
  "transformers==4.44.2",
  "setuptools<81",
]
